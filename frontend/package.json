{
  "name": "swimcrm-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "SwimCRM React SPA for admin, trainer, and client portals.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "budget": "node scripts/check-bundle-budget.mjs",
    "preview": "vite preview",
    "test:smoke": "playwright test"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
